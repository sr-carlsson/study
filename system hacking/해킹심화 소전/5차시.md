PIE
실행할 때 마다 메모리 주소가 바뀜
	- 바이너리의 code 영역, data 영역
ASLR
실행할 때 마다 메모리 주소가 바뀜
	- 스택, 힙, Libc
-server에 걸리기 때문에 바이너리는 영향 x	



실행 전 메모리 주소(오프셋) + 랜덤 메모리 주소(pie base) = 주소 값


read(0, buf, 20) => 1
atoi(buf) => "1" > 1 (str => int)


