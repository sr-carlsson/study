# Telnet 서버 구축
1. TCP 3-Way Handshake<br>
클라이언트 --TCP SYN-> 서버<br>
        <-TCP SYN ACK- <br>
        --TCP ACK --><br>
2. 옵션 협상
    1. WILL 송신 측이 해당 옵션을 활성화하겠음을 알림
    2. WONT 송신 측이 해당 옵션을 활성화하지 않겠음을 알림
    3. DO 수신 측에게 해당 옵션의 활성화를 요청함
    4. DONT` 수신 측에게 해당 옵션의 비활성화를 요청함

dpkg -l | grep telnet으로 현설치된 telnet 관련 패키지 목록 확인<br>
sudo apt install xinetd telnetd 으로 슈퍼 데몬과 telnetd 서버 설치<br>
sudo vi /etc/xinetd.d/telnet 로 telnet 서비스 설정 파일 생성 및 편집<br>

service telnet<br>
{<br>
		disable         = no                  # 서비스 활성화<br>
    flags           = REUSE               # 소켓 재사용 허용 (빠른 재시작)<br>
    socket_type     = stream              # TCP 방식 사용<br>
    wait            = no                  # 동시에 여러 연결 허용<br>
    user            = root                # telnetd 프로세스 root 권한으로 실행<br>
    server          = /usr/sbin/telnetd   # telnet 처리 프로그램 경로<br>
    log_on_failure  += USERID             # 로그인 실패 시 사용자 ID를 로그에 기록<br>
    bind            = 0.0.0.0             # 모든 네트워크 인터페이스에서 접속 허용<br>
}<br>
<img width="812" height="452" alt="image" src="https://github.com/user-attachments/assets/d2515453-e840-4d1e-bc79-701a4d410c3e" />

sudo systemctl restart xinetd 로 xinetd 서비스 재시작하여 변경된 설정 적용<br>
sudo systemctl enable xinetd # 부팅 시 xinetd 자동 시작<br>
<img width="1452" height="292" alt="image" src="https://github.com/user-attachments/assets/619894d5-1f69-4b9e-b87f-3d2dcf2a8e57" />

sudo ufw allow 23/tcp  방화벽에서 23번(telnet) 포트 허용 <br>
sudo ufw enable  방화벽 활성화<br>
<img width="1060" height="266" alt="image" src="https://github.com/user-attachments/assets/4ffe7041-bbf7-496c-a163-8e7799e3d197" />
<br>
